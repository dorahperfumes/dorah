import "./admin.css";

export const metadata = {
  title: "Dorah — Panel de Administración",
};

export default function AdminLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
